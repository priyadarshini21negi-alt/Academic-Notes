#review 
## Random Tensor Generation 
### [[Uniform Random Distribution]] 
`touch.rand()` : generates tensors with values sampled from a Uniform Distribution over the interval $[0,1)$ :
Mathematical Representation : $X$ ~ $U(0,1)$  
- Probability density function: $f(x)=1$ for $x \in [0,1)$
- Mean 
```python
# 3X4 matrix with random values
random_tensor = torch.rand(size=(3,4)) 

#shape
#dim
#min
#max
#mean
 
#Std deviation

```
