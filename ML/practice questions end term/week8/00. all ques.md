Q. Consider a 3-class classification dataset with labels 0,1,0,1, and 22. The data points belong to $\{0,1,2\}^3 \times \{0,1,2\}^3$. If we apply the generative model-based algorithm on the same dataset, how many parameters need to be estimated? Assume that the features given the label are not independent.
	
Q. In previous ques, if the features are conditionally independent given the labels, how many parameters need to be estimated?

Q. ![[Pasted image 20260331152402.png]]
![[Pasted image 20260331152423.png|381]]
![[Pasted image 20260331152514.png|215]]
![[Pasted image 20260331152530.png]]

Q. ![[Pasted image 20260331152551.png]]
What will be the value of $\hat{p}$​?

What will be the value of $\hat{\mu}_0$​?

What will be the value of $\hat{\mu}_1$​?
  
Q. Consider a spam classification problem that was modeled using naive Bayes. The features take a value of 11 or 00 depending on whether a word is present in the email or not. Assume that the probability of a mail being spam is 0.2. The following table gives the estimation for conditional probabilities for some of the words:
![[Pasted image 20260331152614.png]]Consider a mail with the following sentence: "Hurray! win exciting prizes"

With what probability the mail will be predicted spam? Assume that these are the only possible words (that is there are four features) in a mail. Write your answer correct to two decimal places. A


![[Pasted image 20260331152646.png]]



Q1. Consider the two different generative model-based algorithms.

1. Model 1: chances of occurring a feature are affected by the occurrence of other features and the model does not impose any additional condition on conditional independence of features.
2. Model 2: chances of occurring a feature are not affected by the occurrence of other features and therefore, the model assumes that features are conditionally independent of the label.

Which model has more parameters to estimate?

Q.  ![[Pasted image 20260331153206.png]]

![[Pasted image 20260331153219.png]]

Q. Consider a binary classification dataset contains only one feature and the data points given the label follow the given distribution

$$x \mid (y = 0) \sim \mathcal{N}(0, 2), 
\quad x \mid (y = 1) \sim \mathcal{N}(2, \sigma^2)$$

If the decision boundary learned using the gaussian naive Bayes algorithm is linear, what is the value of σ2σ2?

Q. ![[Pasted image 20260331153410.png]]

Q. ![[Pasted image 20260331153429.png]]
![[Pasted image 20260331153438.png]]


Q. ![[Pasted image 20260331153501.png]]

Q. ![[Pasted image 20260331153519.png]]

Q. ![[Pasted image 20260331153655.png]]

Q. ![[Pasted image 20260331153712.png]]

Q. ![[Pasted image 20260331153725.png]]

Q. ![[Pasted image 20260331153741.png]]

Q. ![[Pasted image 20260331153755.png]]

Q. ![[Pasted image 20260331153813.png]]

Q. ![[Pasted image 20260331153830.png]]


Q. Is the following statement true or false?
All the features in the binary classification problem are binary that is they can take only two values.

Q. ![[Pasted image 20260331154022.png]]

Q. Assume that we have modeled the distribution of labels for a given dataset and we want to build the generative model for the same dataset. Which of the following steps would be more appropriate?
Model the distribution 'features given the label'.
Model the distribution 'labels given the feature'.

Q. Consider a binary classification problem with dd binary features. How many parameters have to be estimated if we choose the generative model discussed in the lecture? 
![[Pasted image 20260331154056.png]]

Q. Consider a multi-class classification problem with k classes and d binary features. How many parameters have to be estimated if we choose the generative model discussed in the lecture?
![[Pasted image 20260331154121.png]]

Q. Consider the binary classification problem with dd binary features. Assumes that the features are independent given the labels. How many parameters have to be estimated if we choose the generative model for the dataset?
![[Pasted image 20260331154202.png]]

Q. Which of the following expressions is true if the features f1,f2,...,fd​ are conditionally independent given the label y?
![[Pasted image 20260331154246.png]]

Q. ![[Pasted image 20260331154259.png]]

Q. ![[Pasted image 20260331154323.png]]

Q. ![[Pasted image 20260331154344.png]]
![[Pasted image 20260331154355.png]]

Q. ![[Pasted image 20260331154406.png]]

Q.
![[Pasted image 20260331154418.png]] 

Q. ![[Pasted image 20260331154446.png]]
Q. ![[Pasted image 20260331154455.png]]

Q.   
What will be the value of $\hat{p}_{1,0}$?
$\hat{p}_{j,y}$ is the estimate for the probability that the $j^{\text{th}}$ feature value of an example is $1$, given that the example belongs to label $y$.

Q. ![[Pasted image 20260331155334.png]]

Q. Consider an example of spam classification with binary features discussed in the lecture. A word never appears in the training emails but appears in a test email. What will be the prediction for that email?

spam email with probability 1

non-spam with probability 1

It will be a tie for both spam and non-spam each with a probability of 0.5.

Probabilities for both spam and non-spam will be zero. 

Q. _  
If a certain feature takes only a single value for all the training data points in the naive Bayes classification problem, the prediction of a test point taking the different value for that feature will be indeterminate as the probability of that point taking any label will be zero. What is the possible fix for this issue? Consider that all the features are binary.

Randomly assign a label to that test point.

Add two dummy points with all feature values as 1 in the training dataset. One corresponds to label 00 and another to label 1. Also Add two dummy points with all feature values as 00 in the training dataset. One corresponds to label 0 and another to label 1.


Q. Is the following statement true or false?
If $\hat{p}_{i,0} = 0$ for $y = 0$, then $\hat{p}_{i,1} = 1$ for $y = 1$.

Q. The decision function in a naive Bayes binary classification problem with binary features is:
A. Linear
B. Non-linear
C. Can be either linear or non-linear.

Q. The decision boundary in Naive Bayes is given by:
A. $\{x : P(y = 1 \mid x) = 0\}$
B. $\{x : P(y = 0 \mid x) = 0\}$
C. $\{x : P(y = 1 \mid x) = P(y = 0 \mid x)\}$
D. $\{x : P(y = 1 \mid x) = 1\}$

Q. Is the following statement true or false?
The decision boundary of naive Bayes always passes through the origin.

Q. How many parameters need to be estimated in gaussian naive Bayes for binary classification problem? Assume the same covariances.

Q. What is the estimate for $p = P(y = 1)$ in Gaussian Naive Bayes for a binary classification problem? Assume that $y_i \in \{0,1\}$.

$$
\frac{\sum_{i=1}^{n} y_i}{n}
$$

$$
\frac{\sum_{i=1}^{n} \mathbf{1}(y_i = 1)\, x_i}{\sum_{i=1}^{n} \mathbf{1}(y_i = 1)}
$$

$$
\frac{\sum_{i=1}^{n} \mathbf{1}(y_i = 1)}{n}
$$

$$
\sum_{i=1}^{n} y_i
$$


Q. ![[Pasted image 20260331164118.png]]

Q. ![[Pasted image 20260331164130.png]]

Q. ![[Pasted image 20260331164141.png]]

Q. ![[Pasted image 20260331164153.png]]
