![[Pasted image 20260628105325.png]] ? `<ESC>vyp` - 
- **`<ESC>`** → Exit Insert mode.
- **`vy`** → In Vim, `v` enters **Visual mode** and `y` **yanks (copies)** the selected character.
- **`p`** → Pastes the copied character **after** the cursor. 

What character needs to be pressed when you wish to enter from navigation (normal) mode to insert mode from the beginning of a line in the vi editor? ;; I (capital i) 

What one-word command in navigation mode can be used to delete consecutive 8 lines in vi editor? ;; `8dd`

![[Pasted image 20260628105830.png]] ;; Replaces **UNIX → Linux**, **case-insensitive**, **asks for confirmation**. 

![[Pasted image 20260628105914.png]] ;; ![[Pasted image 20260628110135.png]] 

