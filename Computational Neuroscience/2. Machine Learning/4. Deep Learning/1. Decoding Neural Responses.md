Goals : -
1. Build a deep [[Feed-Forward Network]] using [[PyTorch]] 
2. Evaluate the network's outputs using PyTorch built-in loss functions 
3. Compute gradients of the loss wrt each parameter of the network using automatic [[Differentiation]] 
4. Implement [[Gradient Descent]] to optimize network's parameters 


> Can we look at the activity of thousands on neurons and figure out what the mouse was seeing? 

Steps :- 
<div style="display:flex; gap:20px; align-items:flex-start;">

<div style="flex:1;">

### Steps

- Mouse sees a striped pattern (grating)
- Pattern has some orientation
- ~20,000 neurons in visual cortex respond
- We record those responses
- We give those responses to a neural network
- Neural network tries to guess the orientation

</div>

<div style="flex:1;">

![[]]

</div>

</div>

---
## Decoding models 
### Decoding :-

>Can you reconstruct what the mouse was seeing just from neurons? 

If yes : The information must exist in those neurons 